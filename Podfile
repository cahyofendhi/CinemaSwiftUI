# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CinemaSwiftUI (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CinemaSwiftUI (iOS)
  pod 'Alamofire', '~> 5.5'
  pod 'Kingfisher', '~> 7.0'
  pod 'AlertToast'
  pod 'RealmSwift'
  pod 'SwiftGen', '~> 6.0'

end

target 'CinemaSwiftUI (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CinemaSwiftUI (macOS)

end
